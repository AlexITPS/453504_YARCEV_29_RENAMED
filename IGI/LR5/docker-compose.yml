version: '3.8'

services:
  web:
    build: .
    ports:
      - "9000:8000"   # http://127.0.0.1:9000
    volumes:
      - .:/app
    env_file:
      - .env
    command: python manage.py runserver 0.0.0.0:8000